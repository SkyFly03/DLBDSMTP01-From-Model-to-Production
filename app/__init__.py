# __init__.py
# --------------------------------------------
# Initializes the refund classifier package.
# This can include shared variables or helper functions if needed.
# --------------------------------------------

# Example: Make class names reusable across modules
CLASS_NAMES = ["Bags", "Dresses", "Shoes", "Watches"]
